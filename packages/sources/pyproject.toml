[project]
name = "sources"
version = "0.2.0"
description = "Resource library domain service (repos, documents, pages)"
requires-python = ">=3.11"
dependencies = [
    "platform-actor",
    "platform-capability",
    "platform-contracts",
    "platform-eventbus",
    "platform-secrets",
    "platform-webguard",
    "platform-settings",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/sources"]
