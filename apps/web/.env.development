# Dev environment defaults to the real backend (Vite proxy -> 127.0.0.1:19878)
# Leave empty: the browser requests same-origin /api, and Vite proxies it to the API.
# Never set it to :19877 (that is the services/agent placeholder service, not the main API).
VITE_API_BASE_URL=
